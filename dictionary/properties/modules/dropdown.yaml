color:
  background:
    dropdown:
      select:
        base:
          value: '{color.system.white.value}'

        disabled:
          value: '{color.brand.fog-dark.value}'

      list:
        value: '{color.system.white.value}'

      option:
        hover:
          value: '{color.brand.fog-fade.value}'

  border:
    dropdown:
      select:
        base:
          value: '{color.brand.fog-dark.value}'

        disabled:
          value: '{color.brand.fog-dark.value}'

        focus:
          value: '{color.system.blue.value}'

      list:
        base:
          value: '{color.brand.fog.value}'

  box-shadow:
    dropdown:
      select:
        value: '{color.system.blue-shadow.value}'

      list:
        value: '{color.brand.fog.value}'

  text:
    dropdown:
      select:
        base:
          value: '{color.system.black.value}'

        placeholder:
          value: '{color.brand.fog-dark.value}'

        disabled:
          value: '{color.brand.fog-dark.value}'

font:
  family:
    dropdown:
      select:
        value: '{font.family.custom.value}'

      option:
        value: '{font.family.custom.value}'

  size:
    dropdown:
      select:
        value: 0.8125

      option:
        value: 0.8125

      heading:

border:
  radius:
    dropdown:
      select:
        value: 3px

      list:
        value: 3px
